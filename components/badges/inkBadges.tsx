import { Status } from "@/constants/types";
import React from "react";
import { Text, View } from "react-native";

const InkBadges = ({ icon, status }: { icon: string; status: Status }) => {
  return (
    <View>
      <Text>I</Text>
    </View>
  );
};

export default InkBadges;
