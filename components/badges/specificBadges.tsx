import React from "react";
import { Text, View } from "react-native";

const SpecificBadges = ({ badgeId }: { badgeId: string }) => {
  return (
    <View>
      <Text>specificBadges</Text>
    </View>
  );
};

export default SpecificBadges;
