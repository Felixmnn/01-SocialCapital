import { Relationship } from "@/constants/typesRelationship";
import React from "react";
import { Text, View } from "react-native";

const FriendOverview = ({ relationship }: { relationship: Relationship }) => {
  return (
    <View>
      <Text>FriendOverview</Text>
    </View>
  );
};

export default FriendOverview;
